package tempo

import "strings"

// GENERATED BY POGO. DO NOT EDIT.

// Insert the Event to the database.
func (e *Events) Insert(ee *Event) (event Event, err error) {
	// get all the non-nil fields and prepare them for the query
	c, i, v := querySlices(e.getFields(ee), 0)

	// sql insert query, primary key provided by sequence
	sqlstr := `
	INSERT INTO public.events (` + strings.Join(c, ", ") + `)
	VALUES (` + strings.Join(i, ", ") + `)
	RETURNING "id", "time", "task", "created_at", "triggered_at", "status", "response", "attempts"`

	DBLog(sqlstr, v...)
	row := e.DB.QueryRow(sqlstr, v...)
	err = row.Scan(&event.ID, &event.Time, &event.Task, &event.CreatedAt, &event.TriggeredAt, &event.Status, &event.Response, &event.Attempts)
	if err != nil {
		return event, err
	}

	return event, nil
}
