package tempo

// GENERATED BY POGO. DO NOT EDIT.

// Find a team by id
func (t *Tasks) Find(id *string) (task Task, err error) {
 // sql select query, primary key provided by sequence
 sqlstr := `
    SELECT id, key, target, rate, offset, timezone, rate_type, rate_options, active, target_type, target_options, user, meta, refreshed_at, created_at, updated_at
    FROM public.tasks
    WHERE id = $1`

 DBLog(sqlstr, id)
 row := t.DB.QueryRow(sqlstr, id)
 err = row.Scan(&task.ID, &task.Key, &task.Target, &task.Rate, &task.Offset, &task.Timezone, &task.RateType, &task.RateOptions, &task.Active, &task.TargetType, &task.TargetOptions, &task.User, &task.Meta, &task.RefreshedAt, &task.CreatedAt, &task.UpdatedAt)
 if err != nil {
  return task, err
 }

 return task, nil
}
