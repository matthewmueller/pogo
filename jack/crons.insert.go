package jack

import "strings"

// GENERATED BY POGO. DO NOT EDIT.

// Insert the Cron to the database.
func (c *Crons) Insert(cc *Cron) (cron Cron, err error) {
	// get all the non-nil fields and prepare them for the query
	c, i, v := querySlices(c.getFields(cc), 0)

	// sql insert query, primary key provided by sequence
	sqlstr := `
	INSERT INTO public.crons (` + strings.Join(c, ", ") + `)
	VALUES (` + strings.Join(i, ", ") + `)
	RETURNING "id", "frequency", "job", "tz", "start_time", "end_time", "retry", "created", "updated"`

	DBLog(sqlstr, v...)
	row := c.DB.QueryRow(sqlstr, v...)
	err = row.Scan(&cron.ID, &cron.Frequency, &cron.Job, &cron.Tz, &cron.StartTime, &cron.EndTime, &cron.Retry, &cron.Created, &cron.Updated)
	if err != nil {
		return cron, err
	}

	return cron, nil
}
