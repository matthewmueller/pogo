package jack

// GENERATED BY POGO. DO NOT EDIT.

import (
	"errors"
	"strings"

	"github.com/satori/go.uuid"
)

// Update the Standup by the Primary Key
func (s *Standups) Update(ss *Standup, id *uuid.UUID) (standup Standup, err error) {
	fields := s.getFields(ss)

	// first check if we have the primary key
	if id == nil {
		return standup, errors.New(`primary key "id" must be non-nil`)
	}

	// don't update the primary key
	delete(fields, "id")

	// prepare the slices
	_c, _i, _v := querySlices(fields, 1)

	// sql query
	sqlstr := `UPDATE jack.standups SET (` +
		strings.Join(_c, ", ") + `) = (` +
		strings.Join(_i, ", ") + `)
		WHERE "id" = $1
		RETURNING "id", "name", "slack_channel_id", "time", "timezone", "questions", "team_id", "created_at", "updated_at"`

	// run query
	values := append([]interface{}{id}, _v...)
	DBLog(sqlstr, values...)

	row := s.DB.QueryRow(sqlstr, values...)
	err = row.Scan(&standup.ID, &standup.Name, &standup.SlackChannelID, &standup.Time, &standup.Timezone, &standup.Questions, &standup.TeamID, &standup.CreatedAt, &standup.UpdatedAt)
	if err != nil {
		return standup, err
	}

	return standup, nil
}

// UpdateBySlackChannelID find a Standup
func (s *Standups) UpdateBySlackChannelID(ss *Standup, slackchannelid *string) (standup Standup, err error) {
	fields := s.getFields(ss)

	// first check if we have all the keys we need
	if slackchannelid == nil {
		return standup, errors.New(`slack_channel_id must be non-nil`)
	}

	// don't update the keys
	delete(fields, "slackchannelid")

	// prepare the slices
	_c, _i, _v := querySlices(fields, 1)

	// sql query
	sqlstr := `UPDATE jack.standups SET (` +
		strings.Join(_c, ", ") + `) = (` +
		strings.Join(_i, ", ") + `) ` +
		`WHERE "slack_channel_id" = $1 ` +
		`RETURNING "id", "name", "slack_channel_id", "time", "timezone", "questions", "team_id", "created_at", "updated_at"`

	// run query
	values := []interface{}{}
	values = append(values, slackchannelid)

	values = append(values, _v...)
	DBLog(sqlstr, values...)

	row := s.DB.QueryRow(sqlstr, values...)
	err = row.Scan(&standup.ID, &standup.Name, &standup.SlackChannelID, &standup.Time, &standup.Timezone, &standup.Questions, &standup.TeamID, &standup.CreatedAt, &standup.UpdatedAt)
	if err != nil {
		return standup, err
	}

	return standup, nil
}
