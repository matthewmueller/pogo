package jack

// GENERATED BY POGO. DO NOT EDIT.

import (
	"errors"
	"strings"

	"github.com/matthewmueller/pgx"
)

// Upsert the Standup by the Primary Key
func (s *Standups) Upsert(ss *Standup, action string) (standup Standup, err error) {
	fields := s.getFields(ss)

	// prepare the slices
	_c, _i, _v := querySlices(fields, 0)

	// determine on conflict action
	var upsertAction string
	if action == UpsertDoUpdate {
		upsertAction = `DO UPDATE SET (` + strings.Join(_c, ", ") + `) = ( EXCLUDED.` + strings.Join(_c, ", EXCLUDED.") + `)`
	} else if action == UpsertDoNothing {
		upsertAction = UpsertDoNothing
	} else {
		return standup, errors.New("invalid upsert action")
	}

	// sql query
	sqlstr := `INSERT INTO jack.standups (` + strings.Join(_c, ", ") + `) ` +
		`VALUES (` + strings.Join(_i, ", ") + `) ` +
		`ON CONFLICT ("id") ` +
		upsertAction + ` ` +
		`RETURNING "id", "name", "slack_channel_id", "time", "timezone", "questions", "team_id", "created_at", "updated_at"`

		// run query
	DBLog(sqlstr, _v...)
	row := s.DB.QueryRow(sqlstr, _v...)
	err = row.Scan(&standup.ID, &standup.Name, &standup.SlackChannelID, &standup.Time, &standup.Timezone, &standup.Questions, &standup.TeamID, &standup.CreatedAt, &standup.UpdatedAt)
	if err != nil && err != pgx.ErrNoRows {
		return standup, err
	}

	return standup, nil
}

// UpsertBySlackChannelID find a Standup
func (s *Standups) UpsertBySlackChannelID(ss *Standup, action string) (standup Standup, err error) {
	fields := s.getFields(ss)

	// prepare the slices
	_c, _i, _v := querySlices(fields, 0)

	// determine on conflict action
	var upsertAction string
	if action == UpsertDoUpdate {
		upsertAction = `DO UPDATE SET (` + strings.Join(_c, ", ") + `) = ( EXCLUDED.` + strings.Join(_c, ", EXCLUDED.") + `)`
	} else if action == UpsertDoNothing {
		upsertAction = UpsertDoNothing
	} else {
		return standup, errors.New("invalid upsert action")
	}

	// sql query
	sqlstr := `INSERT INTO jack.standups (` + strings.Join(_c, ", ") + `) ` +
		`VALUES (` + strings.Join(_i, ", ") + `) ` +
		`ON CONFLICT ("slackchannelid") ` +
		upsertAction + ` ` +
		`RETURNING "id", "name", "slack_channel_id", "time", "timezone", "questions", "team_id", "created_at", "updated_at"`

		// run query
	DBLog(sqlstr, _v...)
	row := s.DB.QueryRow(sqlstr, _v...)
	err = row.Scan(&standup.ID, &standup.Name, &standup.SlackChannelID, &standup.Time, &standup.Timezone, &standup.Questions, &standup.TeamID, &standup.CreatedAt, &standup.UpdatedAt)
	if err != nil && err != pgx.ErrNoRows {
		return standup, err
	}

	return standup, nil
}
