package jack

import "strings"

// GENERATED BY POGO. DO NOT EDIT.

// Insert the StandupTeammate to the database.
func (st *StandupTeammates) Insert(stst *StandupTeammate) (standupteammate StandupTeammate, err error) {
	// get all the non-nil fields and prepare them for the query
	_c, _i, _v := querySlices(st.getFields(stst), 0)

	// sql insert query, primary key provided by sequence
	sqlstr := `
	INSERT INTO jack.standups_teammates (` + strings.Join(_c, ", ") + `)
	VALUES (` + strings.Join(_i, ", ") + `)
	RETURNING "standup_id", "teammate_id", "team_owner", "created_at", "updated_at"`

	DBLog(sqlstr, _v...)
	row := st.DB.QueryRow(sqlstr, _v...)
	err = row.Scan(&standupteammate.StandupID, &standupteammate.TeammateID, &standupteammate.TeamOwner, &standupteammate.CreatedAt, &standupteammate.UpdatedAt)
	if err != nil {
		return standupteammate, err
	}

	return standupteammate, nil
}
